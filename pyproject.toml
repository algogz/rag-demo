[project]
name = "bgm-demo"
version = "0.1.0"
description = "BGE-Reranker-v2-m3 multilingual reranking demo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "FlagEmbedding>=1.2",
    "torch>=2.0",
    "transformers>=4.40,<5",
    "sentence-transformers>=2.6",
]
